{
  "name": "pi-model-usage-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first PI-Desktop model usage dashboard plugin",
  "main": "main.js",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
