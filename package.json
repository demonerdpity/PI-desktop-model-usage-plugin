{
  "name": "pi-model-usage-dashboard",
  "version": "0.2.0",
  "private": true,
  "description": "Channel-first quota and local model usage dashboard for PI-Desktop",
  "main": "main.js",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
